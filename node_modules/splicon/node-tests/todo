#XML

-> When passed an icon for each platform, it is inserted with the right props;
-> It creates the platform tag if it does not exist;
-> It does not duplicate tags;
-> It writes to XML;
-> It alerts if XML was not found;
